export interface AccountInfo {
    email: string;
    phone: string;
    createdAt: string;
}

export interface DeviceSession {
    id: string;
    device: string;
    location: string;
    lastSeen: string;
}

