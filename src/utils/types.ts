export interface AccountInfo {
    email: string;
    phone: string;
    createdAt: string;
}

export interface DeviceSession {
    id: string;
    name: string;
    ip: string;
    type: string;
    date: string;
}